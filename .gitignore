bin/
obj/
*.user
.vs/
.vscode/
.idea/

# Local state
*.db
*.db-journal
/local-state/

# Per-developer dev/test helpers (see local/README.md after creation).
/local/

# Runtime preempt scratchpads emitted by the codeybox agent harness.
.codeybox/

# Serilog rolling-file output (see CodeyBox:AuditLog options) and per-agent
# structured-stream logs (see CodeyBox:AgentStreamCapture options).
/logs/
**/logs/*.json
**/logs/agents/

# `dotnet test --logger trx` writes per-run result files here; they're
# time/machine-stamped and not meant to be committed.
**/TestResults/

# ACP bridge native binary produced by scripts/publish-acp-bridge.sh — not
# tracked because it's machine-specific. The tracked placeholder under
# Resources/acp-bridge.placeholder is what ships in dev builds; CI / release
# overwrites it via the publish script.
/src/CodeyBox.Agents.Claude/Resources/acp-bridge
