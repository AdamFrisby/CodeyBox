<Project>

  <PropertyGroup>
    <!-- Solution restore evaluates this before dispatching project restores, so
         the repository config is selected without consulting an inherited user
         profile and remains correct when the solution is built from elsewhere. -->
    <RestoreConfigFile>$(MSBuildThisFileDirectory)NuGet.Config</RestoreConfigFile>
  </PropertyGroup>

</Project>
